# 軽量キューブタイマー 詳細設計書

## 1. 目的

本書は「軽量キューブタイマー 要件定義書」を、実装可能なモジュール、データ、画面、状態遷移および処理手順へ具体化する。

初期バージョンの対象は3×3×3および7×7×7とし、iPhone Safariとホーム画面へ追加したPWAを最優先とする。

## 2. 設計原則

* UIフレームワークを使用せず、HTML、CSS、TypeScriptで構成する
* 実行時の主要外部依存はcubing.jsだけとする
* タイマー計測処理を他の非同期処理から分離する
* 計測中にストレージ操作、スクランブル要求および不要なDOM更新を行わない
* データ保存の成功後に、履歴と次のスクランブルを更新する
* ブラウザAPIは機能検出を行い、利用できない機能によってタイマー全体を停止させない
* 状態の変更は定義済みの遷移関数を通し、イベントハンドラーが直接状態を書き換えない

---

# 3. システム構成

## 3.1 実行構成

```text
index.html
  └─ main.ts
      ├─ AppController
      ├─ TimerController
      ├─ ScrambleService ─ cubing/scramble
      ├─ SolveRepository ─ IndexedDB
      ├─ HistoryController
      ├─ WakeLockService ─ Screen Wake Lock API
      └─ UpdateService ─ Service Worker

sw.ts
  └─ アプリシェルおよびビルド成果物のキャッシュ
```

## 3.2 ディレクトリ構成

```text
/
├─ index.html
├─ package.json
├─ tsconfig.json
├─ vite.config.ts
├─ public/
│  ├─ manifest.webmanifest
│  └─ icons/
│     ├─ icon-192.png
│     ├─ icon-512.png
│     └─ apple-touch-icon.png
├─ src/
│  ├─ main.ts
│  ├─ styles.css
│  ├─ types.ts
│  ├─ constants.ts
│  ├─ app-controller.ts
│  ├─ timer/
│  │  ├─ timer-controller.ts
│  │  ├─ timer-state.ts
│  │  └─ time-format.ts
│  ├─ scramble/
│  │  └─ scramble-service.ts
│  ├─ storage/
│  │  ├─ database.ts
│  │  └─ solve-repository.ts
│  ├─ history/
│  │  └─ history-controller.ts
│  ├─ platform/
│  │  ├─ wake-lock-service.ts
│  │  ├─ visibility-service.ts
│  │  └─ update-service.ts
│  └─ ui/
│     ├─ elements.ts
│     ├─ timer-view.ts
│     ├─ history-view.ts
│     └─ dialog.ts
├─ src/sw.ts
└─ tests/
```

ファイル分割は責務を示すものであり、極端に小さなモジュールを増やさない。循環参照は禁止する。

---

# 4. 型定義

```ts
export type Puzzle = "333" | "777";

export type Penalty = "none" | "plus2" | "dnf";

export interface Solve {
  id?: number;
  puzzle: Puzzle;
  timeMs: number;
  scramble: string;
  createdAt: number;
  penalty: Penalty;
}

export type TimerState =
  | "IDLE"
  | "HOLDING"
  | "READY"
  | "RUNNING"
  | "STOPPED"
  | "CANCELLED"
  | "SAVE_FAILED";

export type AppView = "timer" | "history";

export interface ScrambleSlot {
  puzzle: Puzzle;
  value: string | null;
  requestId: number;
  status: "empty" | "loading" | "ready" | "error";
}

export interface HistoryCursor {
  createdAt: number;
  id: number;
}
```

`SAVE_FAILED`は未保存データが存在する状態を明示し、通常の`STOPPED`と区別する。

## 4.1 定数

```ts
export const HOLD_DURATION_MS = 500;
export const HISTORY_PAGE_SIZE = 50;
export const DB_NAME = "cube-timer";
export const DB_VERSION = 1;
export const SOLVE_STORE_NAME = "solves";
export const LAST_PUZZLE_KEY = "cube-timer:last-puzzle";
```

---

# 5. 画面設計

## 5.1 タイマー画面

画面を次の領域に分割する。

```text
┌──────────────────────────┐
│ 履歴    種目切替 [3×3][7×7]│
├──────────────────────────┤
│ スクランブル              │
│ 複数行表示                │
├──────────────────────────┤
│                          │
│        タイム表示         │
│        状態案内           │
│                          │
│      [+2] [DNF] [削除]    │
└──────────────────────────┘
```

開始前のタイマー操作領域はタイム表示と状態案内を含む中央領域とする。`RUNNING`中は画面全体を覆う停止専用オーバーレイを表示し、その全面を停止用の操作領域とする。

### 表示要素

| 要素 | HTML方針 | 補足 |
|---|---|---|
| 種目切替 | `button`×2 | `aria-pressed`で選択状態を表す |
| スクランブル | `p` | 7×7では小さい文字サイズを適用 |
| タイマー領域 | `button`相当の操作要素 | キーボード操作にも対応する |
| タイム | `output` | 数字幅が変化しないフォント設定 |
| 状態案内 | `p` | `aria-live="polite"`を使用 |
| 直前ソルブ操作 | `div`内の`button`×3 | タイマーカード下部へ配置 |
| 履歴ボタン | `button` | ヘッダーへ配置し、計測中および保存中は無効 |

メイン画面には履歴一覧を置かない。現在種目の直前ソルブをタイマー中央へ表示し、`+2`、`DNF`、`削除`ボタンはタイムから距離を空けてタイマーカード下部へ重ねて配置する。

## 5.2 履歴画面

* 戻るボタン
* 「全種目」「3×3」「7×7」のフィルター
* 履歴一覧
* 追加読み込みボタン
* 空状態または読み込みエラー表示

履歴行はタイム、種目、日時、スクランブル概要、展開ボタン、削除ボタンを持つ。スクランブル全文は初期状態で折りたたむ。

## 5.3 ダイアログ

削除および未保存タイム破棄の確認にはネイティブ`<dialog>`を使用する。利用できない場合は、ページ内のモーダル領域へフォールバックできる構造とする。

## 5.4 レスポンシブ方針

* 375×667 CSSピクセルでスクロールせず主要タイマー操作が可能であることを優先する
* Safe Areaをパディングへ加算する
* 縦方向が不足する場合は、カード間および操作ボタン下部の余白を縮小する
* 7×7スクランブルではスクランブル文字サイズと行間を縮小する
* タイマー数字には`font-variant-numeric: tabular-nums`を使用する
* 状態変化には色だけでなく案内文も使用する

---

# 6. アプリケーション状態

`AppController`が次の状態を保持する。

```ts
interface AppState {
  view: AppView;
  puzzle: Puzzle;
  timerState: TimerState;
  currentScramble: string | null;
  pendingSolve: Solve | null;
  storageReady: boolean;
  historyBusy: boolean;
  updateAvailable: boolean;
}
```

タイマーの高頻度値である開始時刻、Animation Frame ID、直前表示文字列は`TimerController`内部に保持し、アプリ全体の再描画対象にしない。

## 6.1 初期化順序

```text
DOM参照を取得
↓
イベントハンドラーを登録
↓
初期UIを操作不可状態で表示
↓
IndexedDBを開く
├─ 失敗 → 保存不能エラーを表示して計測不可
└─ 成功
    ↓
保存済みの選択種目を復元
↓
最近の履歴を取得
↓
スクランブルを生成
↓
計測操作を有効化
↓
Service Workerを登録
↓
Wake Lockを要求
```

Service WorkerとWake Lockの失敗は初期化全体を失敗させない。

---

# 7. タイマー詳細設計

## 7.1 TimerControllerの責務

* Pointer Eventsをタイマー状態へ変換する
* 長押し用タイマーを管理する
* `performance.now()`による開始・停止時刻を管理する
* `requestAnimationFrame()`による表示値を通知する
* 不正な状態遷移を拒否する
* アプリ非表示時に計測または開始準備を中断する

IndexedDB、スクランブル生成、履歴DOMには依存しない。

## 7.2 内部フィールド

```ts
class TimerController {
  private state: TimerState = "IDLE";
  private activePointerId: number | null = null;
  private holdTimeoutId: number | null = null;
  private startedAt = 0;
  private animationFrameId: number | null = null;
  private lastRenderedText = "";
  private ignoredStopPointerId: number | null = null;
}
```

## 7.3 Pointer Events処理

### IDLEでのpointerdown

1. ストレージが利用可能であることを確認する
2. 有効なスクランブルがあることを確認する
3. 主ポインターかつ未処理の単一ポインターであることを確認する
4. `activePointerId`を保存する
5. `setPointerCapture()`を呼ぶ
6. 状態を`HOLDING`へ変更する
7. 500ms後のタイマーを登録する

### HOLDING中

* 500ms継続した場合は`READY`へ遷移する
* 対応する`pointerup`は長押しタイマーを解除して`IDLE`へ戻す
* `pointercancel`、`lostpointercapture`または操作領域外への移動はキャンセルする
* 追加の`pointerdown`を検知した場合は開始準備をキャンセルする

領域外判定には`pointermove`の座標と`getBoundingClientRect()`を用いる。Pointer Capture中でも視覚上の領域外を判定する。

### READYでのpointerup

1. `performance.now()`を一度呼び、`startedAt`へ格納する
2. 状態を`RUNNING`へ変更する
3. タイム表示を`0.00`にする
4. 次のAnimation Frameを要求する

### RUNNINGでのpointerdown

1. `performance.now()`を一度呼び、停止時刻とする
2. Animation Frameを解除する
3. `elapsedMs = stoppedAt - startedAt`を計算する
4. 状態を`STOPPED`へ変更する
5. 確定表示を同期的に更新する
6. 停止操作のpointer IDを`ignoredStopPointerId`へ一時記録する
7. AppControllerへ停止結果を通知する

停止操作の`pointerdown`に対応する`pointerup`は無視し、その後`ignoredStopPointerId`を解除する。

`RUNNING`へ遷移した時点で、`position: fixed`かつviewport全面を覆う停止専用オーバーレイを表示する。オーバーレイは計測タイムと停止案内だけを表示し、種目切替、履歴およびペナルティ操作を覆う。オーバーレイ上の任意の`pointerdown`を上記停止処理へ渡す。

## 7.4 長押しタイマーの競合防止

`setTimeout()`のコールバック実行時に、状態が`HOLDING`であり、対象pointer IDが現在も有効であることを再確認する。キャンセル時は必ずTimeout IDを解除して`null`へ戻す。

## 7.5 表示更新ループ

```ts
function frame(now: DOMHighResTimeStamp): void {
  if (state !== "RUNNING") return;

  const text = formatTime(now - startedAt);
  if (text !== lastRenderedText) {
    renderTime(text);
    lastRenderedText = text;
  }

  animationFrameId = requestAnimationFrame(frame);
}
```

タイム値はフレーム数の加算で算出しない。バックグラウンド化はVisibilityServiceから同期的に中断通知を受ける。

## 7.6 時間フォーマット

```ts
export function formatTime(timeMs: number): string {
  const centiseconds = Math.floor(Math.max(0, timeMs) / 10);
  const minutes = Math.floor(centiseconds / 6000);
  const seconds = Math.floor((centiseconds % 6000) / 100);
  const fraction = centiseconds % 100;

  return minutes > 0
    ? `${minutes}:${String(seconds).padStart(2, "0")}.${String(fraction).padStart(2, "0")}`
    : `${seconds}.${String(fraction).padStart(2, "0")}`;
}
```

## 7.7 キーボード操作

PCブラウザではSpaceキーをPointer Eventsと同じ状態遷移へ変換する。

* Space押下：`pointerdown`相当
* Space解放：`pointerup`相当
* キーリピートは無視する
* 入力要素またはダイアログにフォーカスがある場合はタイマー操作に使用しない

---

# 8. 計測停止・保存シーケンス

```text
TimerController       AppController       SolveRepository       ScrambleService
      | stopped             |                    |                     |
      |-------------------->|                    |                     |
      | 確定値表示済み       |                    |                     |
      |                     | Solve作成           |                     |
      |                     |------------------->| add                 |
      |                     |                    |                     |
      |                     |<-------------------| id / success        |
      |                     | 最近履歴を更新       |                     |
      |                     |----------------------------------------->| next
      |                     |<-----------------------------------------| scramble
      |                     | IDLEへ遷移          |                     |
```

保存要求中は`pendingSolve`を保持し、種目切替、履歴移動、更新適用および新しい計測を無効にする。

## 8.1 Solve生成

```ts
const solve: Solve = {
  puzzle: currentPuzzle,
  timeMs: elapsedMs,
  scramble: currentScramble,
  createdAt: Date.now(),
  penalty: "none",
};
```

`createdAt`は計測停止後、保存要求を開始する前に一度だけ設定する。再試行時に変更しない。

## 8.2 保存成功

1. 返却された`id`をSolveへ設定する
2. `pendingSolve`を解除する
3. 現在種目の直前ソルブとしてタイマー中央へ表示する
4. 直前ソルブ操作をタイマーカード下部へ表示する
5. 使用済みスクランブルを破棄する
6. 次のスクランブルを要求する
7. 生成成功後に`IDLE`として計測可能にする

## 8.3 保存失敗

1. 状態を`SAVE_FAILED`へ変更する
2. `pendingSolve`を保持する
3. エラー表示と「再試行」「破棄」を表示する
4. `beforeunload`で未保存警告を登録する

再試行では同一Solveを再送する。最初の失敗がトランザクションの結果不明を伴う場合に重複しないよう、Repositoryはトランザクション完了または失敗を明確に待つ。

初期版ではDBが成功を返した後のUI例外を理由に同じSolveを再保存しない。DB成功時点で`pendingSolve`を保存済みとして扱う。

破棄確認後は未保存データを破棄し、使用中だったスクランブルを維持して`IDLE`へ戻る。

---

# 9. IndexedDB詳細設計

## 9.1 DB構造

```text
Database: cube-timer, version 1

Object Store: solves
  keyPath: id
  autoIncrement: true

Indexes:
  createdAt
    keyPath: createdAt
    unique: false

  puzzle
    keyPath: puzzle
    unique: false
```

## 9.2 Database API

```ts
interface SolveRepository {
  open(): Promise<void>;
  add(solve: Solve): Promise<number>;
  delete(id: number): Promise<void>;
  listRecent(puzzle: Puzzle, limit: number): Promise<Solve[]>;
  listPage(
    puzzle: Puzzle | null,
    limit: number,
    cursor?: HistoryCursor,
  ): Promise<{ solves: Solve[]; nextCursor: HistoryCursor | null }>;
}
```

各PromiseはIndexedDB requestの`success`だけでなく、transactionの`complete`で成功確定する。`error`および`abort`を拒否として扱う。

## 9.3 DB初期化

`onupgradeneeded`でObject Storeとインデックスを作成する。既存Storeやインデックスの存在確認を行い、同じバージョンで再作成しない。

`blocked`を検知した場合は、他のタブまたは旧PWAを閉じて再読み込みするよう案内する。`versionchange`を受けた既存接続は閉じる。

## 9.4 履歴ページング

* 全種目では`createdAt`インデックスを`prev`方向へ走査する
* 種目指定時は初期バージョンでは`createdAt`を降順走査し、対象種目だけを採用する
* 50件収集した時点で走査を終了する
* 同一`createdAt`の順序は主キー`id`の降順で安定化する

履歴件数と計測結果を確認し、必要になった場合は将来のDBバージョンで`[puzzle, createdAt]`複合インデックスを追加する。

## 9.5 データ検証

DBから読み込んだデータは最低限以下を確認する。

* `puzzle`が`333`または`777`
* `timeMs`が有限かつ0以上
* `scramble`が文字列
* `createdAt`が有限数
* `penalty`が定義済み値

不正レコードは画面描画から除外し、他の正常レコードの表示を継続する。

---

# 10. スクランブル詳細設計

## 10.1 ScrambleService API

```ts
interface ScrambleService {
  get(puzzle: Puzzle): Promise<string>;
}
```

内部で次を呼び出す。

```ts
import { randomScrambleForEvent } from "cubing/scramble";

const scramble = await randomScrambleForEvent(puzzle);
return scramble.toString();
```

## 10.2 AppControllerでのスロット管理

3×3および7×7ごとに`ScrambleSlot`を1個だけ保持する。大量の事前生成キューは作らない。

```text
生成要求
↓
requestIdをインクリメント
↓
対象スロットをloadingへ変更
↓
cubing.jsへ要求
↓
完了時にrequestIdと種目を再確認
├─ 不一致 → 結果を破棄
└─ 一致 → readyとして保存・表示
```

空文字列または空白だけの結果は生成失敗として扱う。

## 10.3 種目切替

`IDLE`かつ保存処理中でない場合のみ切り替える。

* 切替先スロットが`ready`なら即時表示する
* `empty`なら生成を開始する
* `loading`なら完了を待つ
* `error`なら再試行UIを表示する
* 選択値を`localStorage`へ保存する

`localStorage`の値が不正またはアクセス不能な場合は`333`を初期値とする。localStorage障害はタイマーを停止させない。

---

# 11. 履歴詳細設計

## 11.1 HistoryControllerの責務

* フィルター状態を管理する
* ページ単位でRepositoryから取得する
* 追加読み込みの重複要求を防ぐ
* 削除確認と削除結果を画面へ反映する
* 履歴表示中のWake Lock解除をAppControllerへ要求する

## 11.2 読み込み

フィルター変更時は以下を行う。

1. 進行中の結果を無効化するため読込IDを更新する
2. 一覧とカーソルを初期化する
3. 先頭50件を要求する
4. 現在の読込IDと一致する結果だけを描画する
5. 次カーソルがなければ追加読み込みボタンを隠す

## 11.3 削除

1. 対象Solveのidと表示タイムを確認ダイアログへ表示する
2. 確認後に削除ボタンを無効化する
3. Repositoryの削除完了を待つ
4. 成功時だけDOMとメモリ上の一覧から削除する
5. 直前ソルブに該当する場合は現在種目の最新1件を再取得する
6. 失敗時は行を維持してエラーを表示する

スクランブル文字列は`textContent`で描画し、HTMLとして挿入しない。

## 11.4 直前ソルブのペナルティ

タイマー画面の直前ソルブに限り、`+2`、`DNF`および削除操作をタイマーカード下部に表示する。次のソルブ開始用の中央長押し領域を確保するため、タイム表示の直下には置かない。`IDLE`以外および保存処理中は非表示にする。

* `none`で`+2`を押す：`plus2`
* `none`で`DNF`を押す：`dnf`
* `plus2`で`+2`を押す：`none`
* `dnf`で`DNF`を押す：`none`
* 選択されていないペナルティを押す：そのペナルティへ置換

Repositoryの更新トランザクション完了後に現在種目の最新1件を再取得する。更新中は操作ボタンを無効化し、失敗時は保存済み表示を変更せずエラーを通知する。

---

# 12. Visibility・ライフサイクル設計

## 12.1 visibilitychange

`document.visibilityState === "hidden"`になった場合：

* `HOLDING`または`READY`：開始準備をキャンセルする
* `RUNNING`：計測を中断して`CANCELLED`にする
* Animation Frameと長押しTimeoutを解除する
* Wake Lock参照を破棄する

`visible`へ戻った場合：

* 中断メッセージを表示したうえで`IDLE`へ戻す
* 同じスクランブルを維持する
* タイマー画面ならWake Lock再取得を試みる

## 12.2 pagehide

`pagehide`でも同じ同期的なタイマー中断処理を行う。IndexedDB書き込みなど、新しい非同期処理は開始しない。

保存処理中のページ終了を完全には防止できないため、`pendingSolve`がある場合は`beforeunload`による警告を可能な範囲で設定する。

---

# 13. Wake Lock設計

## 13.1 API

```ts
interface WakeLockService {
  acquire(): Promise<void>;
  release(): Promise<void>;
  handleVisibilityChange(): Promise<void>;
}
```

## 13.2 取得条件

次をすべて満たす場合だけ取得を試みる。

* `navigator.wakeLock`が存在する
* タイマー画面である
* documentがvisibleである
* 既に有効なSentinelを保持していない

取得失敗は内部で記録し、タイマー操作を無効にしない。Sentinelの`release`イベントで参照を`null`にする。

同一イベントから重複取得しないよう、取得中Promiseを1つだけ保持する。

---

# 14. PWA・Service Worker設計

## 14.1 Manifest

```json
{
  "name": "軽量キューブタイマー",
  "short_name": "キューブタイマー",
  "start_url": "/",
  "scope": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#ffffff",
  "icons": []
}
```

実装時には192px、512pxおよびApple Touch Iconを登録する。配信先がサブパスの場合に備え、Viteの`base`とManifestのURLを同じ設定から生成する。

## 14.2 キャッシュ戦略

* ビルド時に生成されたファイル一覧をプリキャッシュ対象とする
* ハッシュ付き静的アセットはCache Firstとする
* HTMLナビゲーションはNetwork Firstとし、失敗時にキャッシュ済みHTMLを返す
* 外部オリジンへの実行時要求を前提にしない
* IndexedDBおよびlocalStorageはService Workerから削除しない

cubing.jsが生成するWorkerや遅延チャンクをオフラインで利用できることをビルド成果物と実機で確認する。初回ロード時に必要なチャンクを取得できない状態を「オフライン利用可能」と表示しない。

## 14.3 更新

新しいService Workerがwaitingになった場合に`updateAvailable`を設定する。

* 通常時は更新バナーを表示する
* `RUNNING`、`STOPPED`、`SAVE_FAILED`および保存中は更新ボタンを無効化する
* 更新実行時にwaiting workerへ適用メッセージを送る
* `controllerchange`後に一度だけ再読み込みする

## 14.4 配信要件

本番環境はHTTPSで配信する。Service WorkerおよびWake LockがSecure Contextを必要とするため、HTTPでの本番運用は対象外とする。ローカル開発はViteのlocalhostを使用する。

---

# 15. エラー処理設計

## 15.1 エラー分類

| 分類 | 例 | タイマー利用 | UI |
|---|---|---:|---|
| 致命的 | IndexedDB初期化失敗 | 不可 | 固定エラーと再読込案内 |
| 保存保留 | Solve保存失敗 | 不可 | 再試行／破棄 |
| 復旧可能 | スクランブル生成失敗 | 不可 | 再試行 |
| 非致命的 | Wake Lock失敗 | 可 | 原則控えめな案内 |
| 非致命的 | Service Worker登録失敗 | 可、オフライン保証なし | オフライン未対応を表示 |
| 操作キャンセル | pointercancel、非表示化 | 可 | 状態案内 |

## 15.2 エラー表示

* ユーザーには短い日本語メッセージと実行可能な次の操作を示す
* 例外オブジェクトの詳細を直接画面表示しない
* 開発時はコンソールへ原因を出力する
* 同じ非致命的エラーを連続表示しない
* 自動再試行はService Worker更新確認など安全な処理に限定する

---

# 16. DOM更新・性能設計

* 初期描画後は画面全体を再構築しない
* タイム表示は`textContent`の差分更新だけを行う
* 履歴は`DocumentFragment`でページ単位に追加する
* 各履歴行へ個別イベントを大量登録せず、一覧でイベント委譲する
* 計測中に日時整形、配列ソート、IndexedDB、スクランブル生成を行わない
* 常時稼働するIntervalを使用しない
* CSSアニメーションは状態変更時の短いものだけとし、`prefers-reduced-motion`へ対応する
* Blur、Canvas、3D描画、背景動画を使用しない

本番ビルドではSource Mapの公開有無を構成で選択し、cubing.jsの不要な表示系モジュールをimportしない。

---

# 17. セキュリティ・プライバシー設計

* 通常利用で外部通信を行わない
* タイム、スクランブル、日時を端末外へ送信しない
* ユーザー入力をHTML文字列として挿入しない
* インラインスクリプトを使用しない
* 外部フォントを読み込まず、システムフォントを使用する
* ログに履歴全件を出力しない
* 将来のホスティングでは可能な範囲でContent Security Policyを設定する

---

# 18. アクセシビリティ設計

* 操作要素にはネイティブbuttonを優先する
* フォーカス表示を消さない
* 状態を色だけで表現しない
* 文字と背景のコントラストを確保する
* タイマー操作をSpaceキーでも実行可能にする
* ダイアログ表示中はフォーカスをダイアログ内に保つ
* 状態案内は`aria-live="polite"`、保存失敗は`role="alert"`を使用する
* ページ全体のピンチズームを禁止しない

---

# 19. テスト設計

## 19.1 単体テスト

### time-format

| 入力 | 期待値 |
|---:|---|
| 0 | `0.00` |
| 9 | `0.00` |
| 10 | `0.01` |
| 12,459 | `12.45` |
| 59,999 | `59.99` |
| 60,000 | `1:00.00` |
| 62,389 | `1:02.38` |

### TimerController

* 500ms未満の保持で開始しない
* 500ms経過後の解放で開始する
* RUNNINGのpointerdownで即時停止する
* 停止後のpointerupを無視する
* pointercancelで開始準備を解除する
* 複数ポインターで開始しない
* 非表示通知でRUNNINGを中断する
* 表示文字列が同じフレームでDOM通知しない

`performance.now()`、Timeout、Animation Frameはテスト用Clockへ差し替えられる構造にする。

### Scramble管理

* 古いrequestIdの結果を反映しない
* 種目が一致しない結果を反映しない
* 空文字列をエラーとして扱う
* 未使用スクランブルを種目別に保持する

### Repository

* DB作成とインデックス作成
* Solve追加と採番
* 種目別の直近取得
* 50件ページング
* 1件削除
* transaction失敗時のPromise拒否

## 19.2 結合テスト

* 起動からスクランブル表示まで
* 計測開始、停止、保存、次スクランブル表示
* 保存失敗、再試行、成功
* 保存失敗後の破棄
* 種目切替と直前ソルブ表示の切替
* 履歴のフィルター、追加読み込み、削除
* 非表示中断後に同じスクランブルで再開
* 更新通知が計測中に適用できないこと

## 19.3 ブラウザ・実機テスト

* iPhone Safari、iOS 16.4以上
* iPhoneホーム画面PWA
* Android Chrome
* PC版Chrome、Edge、Firefox

iPhoneではPointer Events、Safe Area、PWA起動、Wake Lock、visibilitychange、IndexedDB保持、オフライン起動を重点確認する。

## 19.4 負荷・耐久テスト

* 10,000件のテストデータを投入して計測と履歴ページングを確認する
* 100回連続計測し、欠落および重複を確認する
* 3×3と7×7を連続切替して生成結果と応答性を確認する
* 30分連続利用し、発熱、バッテリー、カクつき、操作遅延を記録する
* オフライン状態で再起動し、両種目のスクランブル生成を確認する

---

# 20. 実装順序

1. Vite、TypeScriptおよび静的HTML/CSSの初期構成
2. 型、定数、時間フォーマット
3. TimerControllerと状態表示
4. IndexedDB初期化とSolveRepository
5. 停止から保存までのシーケンス
6. cubing.jsによるスクランブル生成
7. 種目切替と直前ソルブ表示・操作
8. 履歴画面、ページング、削除
9. VisibilityとWake Lock
10. ManifestとService Worker
11. エラー処理と更新UI
12. 単体・結合・実機・負荷テスト

各段階でタイマーの単体テストを維持し、後続機能からTimerControllerへストレージやスクランブルの依存を追加しない。

---

# 21. 要件トレーサビリティ

| 要件 | 詳細設計 |
|---|---|
| 種目切替 | 10.3 |
| スクランブル生成 | 10 |
| タイマー・長押し | 7 |
| 計測精度・表示更新 | 7.5、7.6 |
| タイム保存 | 8、9 |
| 履歴表示・削除 | 11 |
| オフライン・PWA | 14 |
| Wake Lock | 13 |
| 非表示時の中断 | 12 |
| 軽量化・発熱対策 | 16 |
| データ保護 | 8.3、9 |
| 対応環境 | 5.4、19.3 |
| 受け入れ基準 | 19 |

---

# 22. 実装開始時の確定事項

以下は実装時に設定値として確定するが、本設計の変更を必要としない。

* cubing.jsの固定バージョン
* アプリの配色とアイコン意匠
* 本番配信先に応じたViteのbase URL
* Service Workerプリキャッシュ一覧の生成方法
* 単体テストランナーのバージョン

配色とアイコンを除き、上記は最初のプロジェクト構築時にpackage lockおよび設定ファイルへ記録する。
