# 軽量キューブタイマー 要件定義書

## 1. プロジェクト概要

3×3×3および7×7×7に対応した、iPhone向けの軽量なキューブタイマーアプリを開発する。

本アプリでは多機能化よりも、

* 軽快に動作すること
* タイマーとして安定していること
* スマートフォンが発熱しにくいこと
* 操作が単純であること
* オフラインで使用できること

を優先する。

主な想定端末はiPhone SE 第2世代以上とする。

---

# 2. 基本方針

本アプリはWebアプリとして開発し、PWAとしてiPhoneのホーム画面から起動できるようにする。

バックエンドサーバーやユーザーアカウントは使用せず、タイム履歴などのデータは端末内に保存する。

対応種目は初期バージョンでは以下の2種類に限定する。

* 3×3×3
* 7×7×7

機能追加による利便性より、アプリの軽量性・安定性・操作性を優先する。

---

# 3. 対応環境

## 3.1 対象端末

主な対象端末：

* iPhone SE 第2世代以上

SE2以前の古い端末や、極端に古いブラウザへの互換性は保証しない。

---

## 3.2 対応ブラウザ

主対象：

* iPhone Safari
* iPhoneのホーム画面に追加したPWA

可能な範囲で以下でも正常動作する設計とする。

* Chrome
* Edge
* Firefox
* Android Chrome
* PCブラウザ

ただし最優先の動作確認環境はiPhone SafariおよびiPhone PWAとする。

---

# 4. 機能要件

## 4.1 種目切替

以下の2種目を切り替えられること。

* 3×3×3
* 7×7×7

画面上から簡単に切り替え可能とする。

例：

```text
[ 3×3 ]   [ 7×7 ]
```

種目を変更した場合、その種目に対応したスクランブルを表示する。

---

# 4.2 スクランブル生成

3×3および7×7それぞれについて、正しい形式のスクランブルを生成する。

独自の単純なランダム文字列生成は行わず、信頼性のある既存ライブラリを利用する。

スクランブル生成にはcubing.jsを使用する。

使用対象：

* `333`
* `777`

スクランブルはオフライン状態でも生成可能であること。

---

# 4.3 タイマー機能

キューブタイマーとして以下の操作を提供する。

1. タイマー領域に指を置く
2. 一定時間長押しする
3. スタート可能状態になる
4. 指を離す
5. 計測開始
6. 再度画面をタップする
7. 計測停止
8. タイムを保存する

タイマーの状態は内部的に明確に管理する。

想定状態：

```text
IDLE
↓
HOLDING
↓
READY
↓
RUNNING
↓
STOPPED
```

---

# 4.4 長押し判定

誤操作防止のため、タイマー開始前に一定時間の長押しを必要とする。

初期値として約500msを想定する。

初期バージョンではユーザー設定項目にはせず固定値とする。

---

# 4.5 タイム計測精度

時間計測には`performance.now()`を使用する。

画面表示の更新回数からタイムを算出してはならない。

以下の方式とする。

```text
開始時刻を記録
↓
終了時刻を記録
↓
終了時刻 - 開始時刻
↓
最終タイム
```

内部ではミリ秒単位で保持する。

表示は1/100秒単位とする。

表示例：

```text
12.45
```

1分以上の場合：

```text
1:42.38
```

---

# 4.6 タイマー表示更新

計測中の表示更新には`requestAnimationFrame()`を使用する。

ただし、CPU・GPU負荷を抑えるため、不必要なDOM更新は行わない。

表示文字列が変化した場合のみ画面を書き換える。

計測精度は表示更新頻度に依存させない。

---

# 4.7 タイム保存

計測終了後、以下の情報を保存する。

* タイム
* 種目
* 使用したスクランブル
* 計測日時

基本データ構造：

```ts
interface Solve {
    id: number;
    puzzle: "333" | "777";
    timeMs: number;
    scramble: string;
    createdAt: number;
}
```

将来の拡張を考慮し、以下も追加可能な設計とする。

```ts
penalty: "none" | "plus2" | "dnf";
```

初期UIでは直前のソルブに対する+2およびDNF操作を実装する。それ以前の履歴に対するペナルティ編集は初期バージョンの対象外とする。

---

# 4.8 保存先

タイム履歴はIndexedDBを使用して端末内に保存する。

IndexedDBはSafari専用ではなく、Chrome、Firefox、Edgeなど主要ブラウザでも利用可能なWeb標準機能である。

サーバーへの送信は行わない。

---

# 4.9 保存処理の優先順位

計測停止時は、タイム保存を最優先にする。

処理順：

```text
タイマー停止
↓
タイム確定
↓
IndexedDBへ保存
↓
画面更新
↓
次のスクランブル処理
```

スクランブル生成や履歴表示更新を保存処理より先に行わない。

---

# 4.10 履歴表示

保存されたタイムを一覧表示できること。

最低限以下を表示可能とする。

* タイム
* 種目
* スクランブル
* 計測日時

最低限以下の操作を提供する。

* 履歴を見る
* 特定の記録を削除する

高度な統計表示は初期バージョンでは実装しない。

---

# 4.11 スクランブル更新

タイマー停止後に次のスクランブルを表示する。

現在のソルブに使用したスクランブルはSolveデータと一緒に保存するため、次のスクランブルに切り替わっても履歴から確認できること。

---

# 4.12 オフライン動作

一度アプリを読み込んだ後は、インターネット接続がなくても以下の機能を使用できること。

* アプリ起動
* 3×3スクランブル生成
* 7×7スクランブル生成
* タイマー
* タイム保存
* 履歴表示

Service Workerを利用してアプリ本体をキャッシュする。

---

# 4.13 PWA対応

Web App Manifestを使用し、iPhoneのホーム画面に追加できるようにする。

ホーム画面から起動した場合は、ブラウザUIを極力表示せずアプリに近い操作感を提供する。

App Storeへの公開は初期要件に含めない。

---

# 4.14 スリープ防止

Screen Wake Lock APIを使用する。

ただし常時有効にはしない。

タイマー画面を使用している場合のみ必要に応じてWake Lockを取得し、履歴画面などでは解除する。

Wake Lockが使用できない場合でも、タイマー自体は正常動作すること。

---

# 4.15 アプリ非表示時の動作

計測中に以下の状態になった場合の扱いを明確にする。

例：

* ホーム画面へ移動
* 他アプリへ切り替え
* Safariをバックグラウンド化
* PWAを非表示にする

初期仕様では、

**計測中にアプリが非表示になった場合、そのソルブを無効として計測を中断する**

方針を基本とする。

`visibilitychange`などを利用して検知する。

バックグラウンドから復帰した際に、見かけ上タイマーが動き続けていたような状態を作らない。

---

# 5. UI要件

基本画面はできるだけ1画面で完結させる。

イメージ：

```text
┌─────────────────────────┐
│      3×3     7×7        │
│                         │
│ R U2 F' L2 U ...        │
│                         │
│                         │
│          12.43          │
│                         │
│      長押しで開始        │
│                         │
├─────────────────────────┤
│ 最近のタイム             │
│ 12.43                   │
│ 13.02                   │
│ 15.71                   │
│                         │
│        履歴を見る        │
└─────────────────────────┘
```

---

# 5.1 操作領域

タイマー操作領域は十分広く確保する。

小さなボタンを大量に配置しない。

タイマー操作時は以下を防止する。

* 誤スクロール
* テキスト選択
* ダブルタップ拡大
* 不要なブラウザジェスチャー

---

# 5.2 7×7スクランブル表示

7×7のスクランブルは3×3より長いため、複数行表示を前提とする。

iPhone SE2の画面幅で読めることを必須とする。

必要に応じて7×7表示時のみ文字サイズを小さくする。

スクランブル表示領域がタイマー操作を妨害しないようにする。

---

# 6. 軽量化・発熱対策

本アプリではファイルサイズだけでなく、実行中のCPU・GPU負荷を軽量性の重要指標とする。

---

# 6.1 計測中の処理

計測中に行う処理は極力以下だけに限定する。

```text
performance.now()
↓
タイム表示
```

計測中は原則として以下を行わない。

* IndexedDBへの継続的書き込み
* スクランブル生成
* 大量のDOM更新
* ネットワーク通信
* 重いアニメーション
* 3D描画
* Canvasによる常時描画

---

# 6.2 スクランブル生成

cubing.jsのスクランブル機能のみを使用する。

不要な3D表示機能などは利用しない。

cubing.js側のprefetch機能を利用する場合、アプリ側で重複してスクランブルを大量生成しない。

3×3と7×7を頻繁に切り替えた場合のCPU負荷について実機確認を行う。

---

# 6.3 UI描画

派手な常時アニメーションを使用しない。

原則使用しないもの：

* 3Dエフェクト
* 常時動作する背景アニメーション
* Canvas常時描画
* 大量のBlur処理
* 大量の影やGPU負荷の高いエフェクト

タイマーの状態変化については色変更など軽い視覚表現のみ使用する。

---

# 7. 使用技術

## フロントエンド

* HTML
* CSS
* TypeScript

React、Vue、AngularなどのUIフレームワークは使用しない。

---

## 開発・ビルド

* Vite

---

## スクランブル生成

* cubing.js
* `cubing/scramble`

---

## タイマー

* `performance.now()`
* `requestAnimationFrame()`

---

## タッチ操作

* Pointer Events

主に以下を扱う。

* `pointerdown`
* `pointerup`
* `pointercancel`

---

## データ保存

* IndexedDB

外部DBライブラリは使用せず、原則ブラウザ標準APIを使用する。

---

## オフライン

* Service Worker

---

## PWA

* Web App Manifest

---

## スリープ防止

* Screen Wake Lock API

---

# 8. 使用しない技術

初期バージョンでは軽量化のため、以下は原則使用しない。

* React
* Vue
* Angular
* jQuery
* Bootstrap
* Tailwind CSS
* Firebase
* Supabase
* 外部バックエンド
* ユーザー認証システム
* クラウドDB
* 3D描画ライブラリ
* グラフライブラリ

依存ライブラリは最小限とする。

原則として主要な外部依存はcubing.jsのみとする。

---

# 9. 初期バージョンで実装しない機能

以下は初期バージョンの対象外とする。

* ユーザーアカウント
* ログイン
* クラウド同期
* オンラインランキング
* フレンド
* SNS共有
* スマートキューブ連携
* Bluetooth
* 3Dキューブ表示
* Ao5
* Ao12
* 詳細統計
* グラフ
* テーマ大量切替
* 2×2
* 4×4
* 5×5
* 6×6
* 音声
* 広告

---

# 10. 将来拡張として考慮する機能

初期バージョンでは実装しないが、追加しやすい設計としておく。

* Ao5
* Ao12
* PB表示
* CSVエクスポート
* JSONエクスポート
* バックアップ
* 他種目追加

特にローカル保存のみの場合、ブラウザデータ削除や端末変更によって履歴が失われるため、将来的なデータExport機能を考慮する。

---

# 11. 非機能要件

## 11.1 軽量性

iPhone SE 第2世代で快適に使用できること。

操作に明確なカクつきが発生しないこと。

---

## 11.2 発熱

iPhone SE 第2世代で一定時間連続使用した場合でも、アプリによる不要な継続的CPU・GPU負荷を発生させないこと。

目安として30分程度の連続使用テストを実施する。

---

## 11.3 安定性

スクランブル生成や履歴更新によってタイマー計測が阻害されないこと。

タイマー機能を最優先とする。

---

## 11.4 データ保護

タイム停止後は可能な限り速やかにIndexedDBへ保存する。

スクランブル生成などの非重要処理より保存処理を優先する。

---

## 11.5 ネットワーク非依存

通常利用時はネットワーク通信を必要としない。

オフライン状態でも主要機能が利用可能であること。

---

# 12. テスト対象

最低限以下で実機または実環境テストを行う。

### iPhone Safari

* 3×3計測
* 7×7計測
* 履歴保存
* アプリ再読み込み後の履歴保持
* オフライン動作

### iPhone PWA

* ホーム画面から起動
* タイマー操作
* Wake Lock
* バックグラウンド移行
* 復帰動作
* IndexedDB保存

### 負荷テスト

* 30分程度連続利用
* 3×3 / 7×7の切替
* 多数のタイム保存
* 7×7スクランブル連続生成

確認項目：

* 発熱
* バッテリー消費
* カクつき
* 操作遅延
* タイム計測異常
* 履歴消失

---

# 13. 開発優先順位

開発では以下の順番を優先する。

1. タイマーの正確性
2. タイマー操作の安定性
3. スクランブルの正しさ
4. タイム保存の確実性
5. 軽量性・発熱対策
6. 操作性
7. オフライン動作
8. 見た目
9. 追加機能

---

# 14. 最終コンセプト

本アプリは、

**「3×3・7×7に対応し、iPhone SE2でも軽快かつ発熱を抑えて使える、オフライン完結型のシンプルなキューブタイマー」**

を目標とする。

高機能な競技管理アプリではなく、

**アプリを開く → スクランブルする → すぐ計測する**

という基本体験を最優先にする。

---

# 15. 詳細動作仕様

本章では、設計および実装時の判断差異を防ぐため、タイマー操作、例外処理、データ保存などの詳細動作を定義する。

## 15.1 タイマー操作

タイマー操作は以下の方式とする。

* 開始準備はタイマー操作領域の`pointerdown`で開始する
* 500ms経過すると`READY`へ遷移する
* `READY`後の`pointerup`で計測を開始する
* 500ms未満で指を離した場合は`IDLE`へ戻る
* 計測停止はタイマー操作領域の`pointerdown`で判定する
* 停止後の`pointerup`では追加の処理を行わない
* `HOLDING`中の`pointercancel`は開始キャンセルとする
* `HOLDING`中に指が操作領域外へ出た場合も開始キャンセルとする
* 複数指による操作は無効とする
* 計測中は種目切替、履歴表示、履歴削除などの操作を無効化する
* Pointer Captureを使用して開始操作を安定して追跡する
* `RUNNING`中は画面全体を覆う停止専用画面を表示する
* 停止専用画面の任意の位置で発生した`pointerdown`を停止操作として扱う

停止を`pointerdown`で判定し、指を離すまでの時間が計測値に含まれないようにする。

状態遷移は以下を基本とする。

```text
IDLE
  ↓ pointerdown
HOLDING
  ↓ 500ms経過
READY
  ↓ pointerup
RUNNING
  ↓ pointerdown
STOPPED
  ↓ 保存成功
IDLE
```

例外処理用の内部状態として`CANCELLED`を追加する。

## 15.2 タイマー状態の表示

状態ごとに色と案内文を変更する。

* `IDLE`：通常色、「長押しで開始」
* `HOLDING`：注意色、「そのまま長押し」
* `READY`：開始可能色、「離してスタート」
* `RUNNING`：計測中を示す色、案内文は原則非表示
* `STOPPED`：確定したタイムを表示
* `CANCELLED`：中断理由を短く表示

振動および音声は初期バージョンでは使用しない。

## 15.3 アプリ非表示時の動作

計測中にアプリが非表示になった場合は以下のように扱う。

* 対象ソルブを破棄し、履歴には保存しない
* `RUNNING`から`CANCELLED`へ遷移する
* `requestAnimationFrame()`による表示更新を停止する
* 復帰時に`IDLE`へ戻す
* 「画面が非表示になったため計測を中断しました」と表示する
* 使用していたスクランブルは維持する
* 次のスクランブルは生成しない
* 同じスクランブルでもう一度計測可能とする

`HOLDING`または`READY`中にアプリが非表示になった場合は、開始操作をキャンセルして`IDLE`へ戻す。

非表示状態の検知には主に`visibilitychange`を使用し、`pagehide`も補助的に使用する。

## 15.4 タイムの計算と表示

* 計測値は`performance.now()`で取得した開始時刻と停止時刻の差分とする
* 内部および履歴ではミリ秒単位で保持する
* 表示時は1/100秒単位で切り捨てる
* 計測中表示と履歴表示では同じフォーマット関数を使用する

表示例：

```text
12,459ms → 12.45
62,389ms → 1:02.38
```

計測中は、フォーマット後の表示文字列が前回から変化した場合のみDOMを更新する。

## 15.5 計測停止後の保存処理

停止時は以下の順序で処理する。

```text
停止時刻を取得
↓
タイムを確定して画面に表示
↓
Solveオブジェクトをメモリ上に保持
↓
IndexedDBへ保存
↓
履歴表示を更新
↓
次のスクランブルを要求
↓
次の計測を有効化
```

確定タイムはIndexedDBへの保存完了を待たずに表示する。履歴表示の更新および次のスクランブルへの移行は、保存成功後に行う。

## 15.6 保存失敗時の動作

IndexedDBへの保存に失敗した場合は以下のように扱う。

* 確定タイムをメモリ上に保持する
* 次のスクランブルへ進まない
* 新しい計測を開始できないようにする
* 「タイムを保存できませんでした」と表示する
* 「再試行」と「破棄」の操作を提供する
* 再試行に成功した場合は通常処理へ復帰する
* 破棄には確認操作を設ける
* ページを閉じると未保存タイムが失われる可能性があることを表示する

初回のIndexedDB初期化に失敗した場合は、「履歴を保存できないため計測を開始できません」と表示し、通常計測を開始できないようにする。

---

# 16. データ詳細仕様

## 16.1 Solveデータ

初期バージョンから`penalty`を含め、以下の構造とする。

```ts
interface Solve {
    id?: number;
    puzzle: "333" | "777";
    timeMs: number;
    scramble: string;
    createdAt: number;
    penalty: "none" | "plus2" | "dnf";
}
```

計測停止時の初期値は`penalty: "none"`とし、直前ソルブの操作により`plus2`または`dnf`へ更新可能とする。`id`はIndexedDBへの保存前には存在しないため任意プロパティとする。

## 16.2 IndexedDB構成

IndexedDBは以下の構成とする。

* DB名：`cube-timer`
* Object Store名：`solves`
* 主キー：`id`
* キー生成：`autoIncrement: true`
* 初期DBバージョン：`1`
* `createdAt`インデックスを作成する
* `puzzle`インデックスを作成する
* 履歴は`createdAt`の降順で取得する
* 将来の構造変更時はDBバージョンを上げてマイグレーションする

同じミリ秒に複数件が保存される可能性があるため、表示順が同一となる場合は`id`の降順を第二の並び順として扱う。

---

# 17. スクランブル詳細仕様

## 17.1 生成方法

* `randomScrambleForEvent("333" | "777")`を使用する
* 生成されたAlgを`.toString()`して表示および保存する
* cubing.jsはnpm依存としてアプリへ組み込み、実行時にCDNから取得しない
* cubing.jsのバージョンを固定する
* 有効なスクランブルが存在しない間は計測を開始できないようにする
* 初回生成中は「スクランブル生成中」と表示する
* 生成失敗時はエラーと「再試行」を表示する
* 自動的な無限再試行は行わない

## 17.2 非同期処理の競合防止

スクランブル生成要求ごとに連番または同等の識別子を持たせる。

生成結果を適用する時点で現在の要求および種目と一致することを確認し、古い非同期処理の結果を画面へ反映しない。

計測中はアプリ側から新しいスクランブル生成を要求しない。cubing.jsのprefetchは初期実装では既定動作を利用し、3×3と7×7の切替を含む実機負荷試験で問題が確認された場合は無効化する。

## 17.3 種目切替

* 種目を切り替えられるのは`IDLE`時のみとする
* `HOLDING`、`READY`、`RUNNING`および保存処理中は切替不可とする
* 種目変更後は対応するスクランブルを表示する
* 種目ごとに直前の未使用スクランブルを保持する
* 別種目へ戻った場合、スクランブルが未使用であれば再表示する
* 計測に使用して保存したスクランブルは再利用しない
* 最後に選択した種目を`localStorage`へ保存する

タイム履歴本体はIndexedDBのみに保存する。

---

# 18. 履歴詳細仕様

* タイマー画面には現在の種目の直近5件を表示する
* 履歴画面の初期表示対象は全種目とする
* 全種目、3×3、7×7で絞り込み可能とする
* 新しい記録から順に表示する
* 初回は最大50件を表示する
* それ以降はユーザー操作により50件ずつ追加読み込みする
* 全履歴を一度にDOMへ描画しない
* スクランブルは初期状態では省略表示し、操作により全文を表示可能とする
* 計測日時は端末のローカルタイムで表示する
* 削除前に確認を表示する
* 削除に失敗した場合は一覧から消さず、エラーを表示する

初期バージョンの削除操作は1件ずつとし、全件削除機能は実装しない。

## 18.1 直前ソルブの操作

タイマー画面に表示する直前のソルブに対して、以下の操作を提供する。

* `+2`ペナルティの設定および解除
* `DNF`ペナルティの設定および解除
* 記録の削除

`+2`と`DNF`は同時に設定せず、後から選択した方を適用する。選択中のペナルティをもう一度押した場合は`none`へ戻す。

表示は以下とする。

* `none`：計測タイムをそのまま表示する
* `plus2`：計測タイムへ2秒加算し、末尾に`+`を表示する
* `dnf`：`DNF`と元の計測タイムを表示する

ペナルティ変更および削除はIndexedDBの更新完了後に画面へ反映する。削除前には確認を表示する。

---

# 19. PWA・オフライン詳細仕様

## 19.1 キャッシュ対象

Service Workerでは以下をオフライン利用可能にする。

* HTML
* CSS
* アプリのJavaScript
* cubing.jsを含むビルド成果物
* Workerおよび遅延ロードされるJavaScriptチャンク
* Web App Manifest
* アプリアイコン
* その他、通常利用に必要なローカル静的アセット

初回の読み込みおよび必要アセットのキャッシュが正常に完了した後を「オフライン利用可能」とする。初回アクセス前の完全なオフライン起動は保証しない。

## 19.2 アプリ更新

* 新しいService Workerはバックグラウンドで取得する
* 使用中の画面を強制的に再読み込みしない
* 更新を検知した場合は「新しいバージョンがあります」と表示する
* `RUNNING`中および保存処理中は更新操作を無効化する
* ユーザーによる更新操作または次回起動時に新バージョンを適用する
* Service Workerの更新処理によってIndexedDBを削除しない

---

# 20. Wake Lock詳細仕様

* タイマー画面が表示されている間のみWake Lockを取得する
* 履歴画面ではWake Lockを解除する
* アプリが非表示になった場合は、Wake Lockが解除されたものとして参照を破棄する
* アプリ復帰時は必要に応じてWake Lockの再取得を試みる
* 対応判定には`"wakeLock" in navigator`を使用する
* 低電力モード、OSまたはブラウザの判断による取得拒否を想定して例外処理する
* Wake Lockの取得および再取得の失敗は非致命的なものとして扱う
* Wake Lockを利用できない場合でもタイマーを使用可能とする

Wake Lockを利用できない環境では、端末画面が自動消灯しないことは保証しない。

---

# 21. 対応環境の詳細

主対象環境は以下とする。

* iPhone SE 第2世代以上
* iOS 16.4以上
* 最新のiOSを推奨する
* AndroidおよびPCブラウザはベストエフォート対応とする

---

# 22. タッチ操作・レイアウト詳細

## 22.1 ブラウザジェスチャー対策

タイマー操作領域に限定して以下を適用する。

* `touch-action: none`
* `user-select: none`
* `-webkit-user-select: none`
* 必要な範囲でコンテキストメニューを抑止する
* Pointer Eventsの不要な既定動作を抑止する

ページ全体のズームを全面的に禁止せず、タイマー領域におけるダブルタップなどの誤操作を防止する。

## 22.2 レイアウト

* 縦持ちを正式な主対応とする
* 横持ちは利用可能とするが、最適表示は保証しない
* `env(safe-area-inset-*)`を使用してSafe Areaへ対応する
* iPhone SE 第2世代の375×667 CSSピクセルを基準として表示確認する
* 7×7スクランブルは複数行に折り返して表示する
* スクランブル表示によってタイマー操作領域の必要な最小高さが失われないようにする
* OSのライトモードおよびダークモード設定に追従する
* 独自のテーマ選択機能は初期バージョンでは実装しない
* 常時動作するアニメーションは使用しない

---

# 23. 受け入れ基準

初期バージョンは最低限以下を満たすこと。

* 100回連続して計測し、履歴の欠落および重複保存が発生しない
* 10,000件保存済みの状態でも計測操作に明確な遅延が発生しない
* 計測中にアプリ側からスクランブル生成およびIndexedDB書き込みを開始しない
* アプリ非表示によって中断された計測が履歴へ保存されない
* タイムの保存成功前に次のスクランブルへ進まない
* 種目を高速に切り替えても異なる種目のスクランブルが表示されない
* 一度正常に読み込んだ後は、オフラインで再起動して両種目のスクランブルを生成できる
* Service Worker更新後もIndexedDBの履歴が維持される
* iPhone SE 第2世代で30分連続使用し、操作不能、明確なカクつき、異常終了が発生しない
* 30分連続試験は同一端末、同程度の画面輝度、非充電状態など、可能な範囲で同じ条件にそろえる
* 履歴が10,000件存在する場合でも、一度に描画する履歴のDOM要素は原則50件以内とする
